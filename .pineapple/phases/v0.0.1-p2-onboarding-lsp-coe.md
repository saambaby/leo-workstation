# COE — `v0.0.1-p2-onboarding-lsp`

**Date:** 2026-07-12  
**Ledger fold:** `ledger.py coe --phase v0.0.1-p2-onboarding-lsp` → **0** classified
task failures (no `task_failed` / stall / lease / assembly-fail / budget events).

Product path (LSP-T-01/02) merged clean; final assembly-check all pass. Escapes were
**process/host**, noticed by the coordinator during dispatch — not by a human
acceptance walk of the Flutter UI.

## Incidents → guards

| Incident | Class | Guard |
|---|---|---|
| `cursor-agent --worktree pin-31/signup-lsp-wire` rejected (`/` illegal) | env | **FM-PROCESS-3** — sanitize worktree name; keep slash only on git branch |
| Agent exited after “Using worktree…” with no work (json + nohup) | flake/env | **FM-PROCESS-4** — 30s liveness / re-dispatch with workspace+text |
| Work order owned `onboarding_routes.dart`; taskgraph `owns` did not | spec-drift | **FM-PROCESS-5** — sync taskgraph owns or block Gate 2 |
| Ledger looked “done” before human merge | process | Reinforce **FM-PROCESS-2** — only `merge` after `MERGED` |

## Alarm

Upstream gates were **not** theatre for product defects (assembly stayed green;
no bad code reached humans). Process dispatch needed two retries on T-01 before a
healthy worker — FM-PROCESS-3/4 close that hole for the next Cursor orchestrate.

## Deferred (not a COE defect)

LSP-T-03 staging E2E (S1–S7) skipped by operator — tracked as open item, not a
missing guard.
