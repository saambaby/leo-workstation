# P2 delta — `v0.0.1-p2-onboarding-lsp` — In-app LSP signup

**Goal:** *Can an LSP operator complete account creation in-app — picker → details →
OTP verify → MFA enroll — and land on `/dispatch`, without leaving the app?*

**Status:** ✅ **complete** (2026-07-12) — LSP-T-01/02 merged; LSP-T-03 E2E smoke
**deferred** (operator skip). Taskgraph:
[`v0.0.1-p2-onboarding-lsp-taskgraph.md`](v0.0.1-p2-onboarding-lsp-taskgraph.md).
Shipped as a **separate pass** after `v0.0.1-p2-onboarding` (founder scheduling call
resolved by orchestration).

## In-scope

| Slice | Delivers | Spec |
|---|---|---|
| `core/auth` (wire) | `signupLsp`, `SignupPath.lsp`, LSP request DTO | [`lsp-native-signup.md`](../features/lsp-native-signup.md) |
| `features/onboarding` (presentation) | LSP card in-app push, LSP fields on details screen incl. required `baa_ack`, `SignupNotifier.submitLsp` | [`lsp-native-signup.md`](../features/lsp-native-signup.md) |

## Out-of-scope

- LSP languages/partners/pricing onboarding wizard (stays `leo-web`)
- Any new `leo-api` route or DTO change (backend union already accepts `business_type: 'lsp'`)
- Removing the `leo-web` LSP signup entry point (stays as an alternate)
- Admin CRUD / rates / billing / users routes (`INV-CLIENT-ROUTE-1` back-office half, unchanged)

## Backend dependency

None new. Reuses `leo-api` alpha.4+'s existing `POST /auth/signup` union
(`business_type: 'lsp'`), `POST /auth/verify-email`, and `POST /auth/mfa/enroll` —
all already wired by `v0.0.1-p2-onboarding` / `v0.0.1-alpha.1-auth-live`.

## Done-when

- [x] LSP-T-01..02 acceptance criteria met (PR [#35](https://github.com/saambaby/leo-workstation/pull/35), [#36](https://github.com/saambaby/leo-workstation/pull/36))
- [x] `flutter analyze` clean on touched paths (per task verify scripts)
- [ ] Manual E2E smoke (LSP-T-03) — **deferred** (operator skip 2026-07-12); re-run S1–S7 against staging when convenient

## E2E smoke log (LSP-T-03)

- Date: 2026-07-12
- Staging API: n/a (deferred)
- Results: S1..S7 deferred (operator skip)
- Notes: Code path closed; smoke not blocking phase close.
