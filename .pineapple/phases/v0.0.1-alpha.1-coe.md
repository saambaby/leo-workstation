# COE — `v0.0.1-alpha.1` + onboarding acceptance walk

_Date: 2026-06-30 · Human acceptance walk (not CI/reviewer)._

## Alarm

**All listed bugs reached the acceptance walk.** `flutter analyze` stayed clean; router
unit tests (641-combination loop-safety, redirect table) passed; no widget/golden tests
exist (`INV-CLIENT-TEST-1`). Reviewer and assembly gates did not exercise macOS window
integration, auth form layout, or GoRouter shell placement. **Upstream gates were
theatre for this slice** — they ran but did not gate UI/platform integration.

Recommendation: add a short **manual acceptance checklist** to release-checklists for
macOS desktop (hidden title bar, signup consent row, signup → login navigation) until
golden or integration tests are explicitly requested.

---

## Bugs (acceptance walk only)

| # | Bug | Root cause | Fix (as-built) | Pattern |
|---|---|---|---|---|
| B1 | Consent checkboxes overlap diagonally on signup details | `LeoCheckbox` used `CupertinoButton(minimumSize: Size.zero)` in stretch `Column` → zero layout height | `GestureDetector` + vertical padding; `Align` left in form | **FM-CLIENT-1** (new) |
| B2 | OTP row misaligned with label on forgot-password verify | Row centered / variable cell sizing vs design | `MainAxisAlignment.start`, fixed cell dimensions | **FM-CLIENT-6** (new) |
| B3 | “Create account” bounced to sign-in | Signup routes missing from `publicRoutes` / redirect | Added `/signup`, `/signup/details`, `/verify-email` to public + auth-flow sets | Existing router table gap — extend redirect tests when new public routes added |
| B4 | Forgot-password flow dead-ended at “check email” | Product shape mismatch (not guard failure) | verify → OTP → reset → login | N/A (spec/UX correction) |
| B5 | macOS title bar showed `leo_workstation`; wanted custom frame | Platform `CFBundleName` / `android:label` + visible system title | Hidden title bar in `MainFlutterWindow.swift`; display name → `leo workstation` | **FM-CLIENT-5** (new) |
| B6 | `GoRouter` not found in context | `DesktopWindowChrome` in `CupertinoApp.builder` above inherited router | Moved frame to root `ShellRoute` | **FM-CLIENT-2** (new) |
| B7 | `setState()` during build on launch | `ListenableBuilder` on `GoRouterDelegate` during route restore | Same as B6 — shell rebuilds without delegate listener | **FM-CLIENT-3** (new) |
| B8 | `GoError: There is nothing to pop` on sign-in path | Toolbar back visible when `canPop()` true after `go('/login')` from signup | Removed global toolbar back; auth uses in-screen links | **FM-CLIENT-4** (new) · maps to catalogue **#4** affordance-vs-stack |

_Bugs caught before acceptance (not COE scope): redirect loop-safety, auth notifier
contract, analyze/lint._

---

## Guards installed

| Guard | Where |
|---|---|
| FM-CLIENT-1 … 6 | [`.pineapple/failure-modes.md`](../failure-modes.md) |
| Root app chrome must use `ShellRoute`, not `CupertinoApp.builder`, for GoRouter access | [`features/router.md`](../features/router.md) § Approach |
| macOS window frame: hidden system title, traffic-light inset, no global auth back | [`features/core-shell.md`](../features/core-shell.md) § Acceptance criteria |
| `DesktopWorkstationShell` vs `WorkstationScaffold` documented | [`features/core-shell.md`](../features/core-shell.md) · [`code-map.md`](../code-map.md) |
| As-built: onboarding + desktop shell | [`state.md`](../state.md) |

---

## Unmatched → new patterns

All acceptance bugs mapped to FM-CLIENT-1 … 6 (see failure-modes catalogue).

---

## Follow-ups (not guards)

- [ ] Manual macOS checklist in `docs/release-checklists.md` § alpha.1
- [ ] Optional: golden test for signup consent row if user requests tests
- [ ] Rename `DesktopWorkstationShell` → `MacosWindowFrame` if team wants less naming collision (cosmetic)
