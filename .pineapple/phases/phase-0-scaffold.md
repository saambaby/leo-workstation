# Phase 0 — Scaffold (historical)

_Status: ✅ complete. Kept as historical record._

The pre-P1 baseline: a Flutter app scaffold with a Cupertino placeholder.

## Delivered

- `lib/main.dart` — `CupertinoApp` → `WorkstationShell` placeholder.
- Default Flutter platform folders (android/ios/macos/linux/windows/web).
- `pubspec.yaml` with `flutter`, `cupertino_icons`, `flutter_lints`.
- Cupertino-first baseline established (`INV-CLIENT-UI-1`).

## Not in this phase

No auth, router, providers, features, or networking. Those begin at
[`v0.0.1-alpha.1`](v0.0.1-alpha.1.md) (P1 app shell).
